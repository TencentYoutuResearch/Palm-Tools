# Tasks

- [ ] 在 `app.css` 的 `.root` 或 `body` 添加 `user-select: none`
- [ ] 在 `SpecPageView.svelte` 的 `.spec-block > .markdown` 添加 `user-select: text`
- [ ] 在 `Markdown.svelte` 的 `.markdown` 添加 `user-select: text`
- [ ] 目视确认 SpecPageView 文档正文可选中、UI 装饰不可选中
- [ ] 目视确认 Chat 消息 markdown 可选中、session 列表/分组头不可选中
- [ ] 目视确认 Rail、StatusBar、DocTree 不可选中
- [ ] 跑 `pnpm check` 确认无 CSS 语法或类型错误
- [ ] 跑 `pnpm test` 确认无回归
