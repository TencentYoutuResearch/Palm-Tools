# Tasks

- [x] 在 DocTree.svelte 中添加 `statusPriority` 映射（active=0, proposed=1, completed=2, draft=3, archived=4）
- [x] 在 `groups` derived 中对每个 bucket 的 entries 按 status 优先级排序
- [x] 运行 `pnpm test` 确认不破坏现有测试（175 passed）
