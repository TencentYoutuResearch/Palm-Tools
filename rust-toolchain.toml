[toolchain]
# Project-pinned toolchain. rustup will fetch this on demand without
# changing the user's global default.
# Requires edition 2024 stabilization (Rust 1.85+).
channel = "1.89"
components = ["rustfmt", "clippy"]
